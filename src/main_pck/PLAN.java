package main_pck;

public class PLAN {

//STEPS:
//1.Chess board orientation(coordinates)
//2.Each piece(their right movements): Pawn, rook, bishop, knight, queen, king
//3.Taking piece(what can take what)
//4.Display

	/*
	 * THINGS DONE FOR TAKING METHOD:
	 * 
	 * -white: pawn(done), queen(done), rook(done), knight(done), bishop(done),
	 * king(done)
	 * 
	 * -black: pawn(done), queen(done), rook(done), knight(done), bishop(done),
	 * king(done)
	 * 
	 * -main running method
	 * 
	 * -castle method(DONE I THINK)
	 * 
	 * -if pawn reaches end(done)
	 * 
	 */
}
